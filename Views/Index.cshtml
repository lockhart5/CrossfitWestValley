@{
    PageData["title"] = "AngularJS SPA Template for Visual Studio";
}
<h2>Welcome!</h2>
<p>This is just a demo page.</p>
<div id="messagesDiv">
    <div ng-repeat="ath in athletes">{{ath.firstname}} {{ath.lastname}}</div>
</div>